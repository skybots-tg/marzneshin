# Operational notes

## 2026-06-12 — UNIVERSAL 1: отключены нерабочие хосты

После диагностики (сервер исправен: reality-хендшейк + egress работают со всех нод, проверено end-to-end с нод 25/31; внешняя TCP-достижимость всех портов с ноды в Норвегии OK), у клиента на бытовом провайдере (РФ) трафик не шёл на части локаций даже после смены портов (random и `*443`). Хендшейк/пинг проходил, реальный трафик — нет. Воспроизвести с доступных RU-вантажей нельзя: ноды 25/31 — обе AdminVPS, трафик между ними не проходит через тот же TSPU/DPI, что у клиента.

Решение: временно отключить нерабочие хосты в `hosts` (`is_disabled=1`).

Отключённые хосты UNIVERSAL 1 (id → remark):
- 78  — RU (Я за границей)
- 93  — RU xhttp
- 359 — PL
- 367 — PL xhttp
- 360 — NL
- 361 — DE (GE-1)
- 362 — DE-2 (GE-2)
- 366 — DE xhttp
- 363 — FI-2 (egress FI-1)
- 364 — FI-3 (egress FI-2)
- 365 — US

Ранее уже были отключены: EE (83,355), FI/FI xhttp (80,94), RO (404).

Оставлены включёнными (рабочие, проверены): FR (79), FR xhttp (96), FR-2 (358), TR (356), US-2 (357).
Им выставлен `weight=110` — вровень с «UNIVERSAL 2 RU xhttp» (host 409, weight 110).

### Текущие порты бриджей на ноде 25 (89.191.225.218) после ремапа
Менялись слушающий порт на ноде 25 + `inbounds.config.port` в панели (хосты с `port=NULL` наследуют от inbound). Новые порты (на момент заметки слушаются, но у клиента трафик не пошёл):
- PL 269 → 27814, NL 270 → 33621, DE/GE-1 271 → 41205,
- DE-2/GE-2 272 → 26443, FI-1 273 → 34443, FI-2 274 → 37443, US 275 → 28443.

### TODO / куда копать дальше
- US-бридж: на egress-ноде рассинхрон reality-ключей (`us-out`), отдельно чинить.
- Корень проблемы клиента не локализован server-side; вероятно специфика пути клиент→нода25 (DPI/маршрут), который недоступен для теста с AdminVPS-вантажей.
